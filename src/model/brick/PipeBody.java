package model.brick;

import java.awt.image.BufferedImage;

/**
 * The pipe's body.
 *
 * @version 1.0.0
 * @see Brick
 */
public class PipeBody extends Brick {
    public PipeBody(double x, double y, BufferedImage style) {
        super(x, y, style);
    }
}
